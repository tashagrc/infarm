import 'package:infarm/constants/constantBuilder.dart';

class CartPage extends StatelessWidget {
  const CartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Halaman Keranjang"),),
    );
  }
}